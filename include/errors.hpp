#pragma once

namespace errors {
inline constexpr const char* kWrongArgument =
    "Wrong arguments! You must use --file <filename> and --word <word>";
inline constexpr const char* kFailedToOpen = "Failed to open file!";
};  // namespace errors
